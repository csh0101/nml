# 标签: 小说项目

## 作用
NML 文档根标签，声明项目级信息。

## 父标签
无（根标签）。

## 子标签
- 元数据（必需）
- 设定集（可选但推荐）
- 正文（可选但推荐）
- 验收规则（可选）

## 属性
- 名称: `string`，必填。小说项目名。
- 版本: `string`，必填。规范版本，例如 `2.1-M1`。

## 约束
- 文档必须且仅有一个 `小说项目`。
