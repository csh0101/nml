# 标签: 场景

## 作用
最小执行单元，Agent 一次仅处理一个场景。

## 父标签
- 章节

## 子标签
- 任务（建议 1 个）
- 配比（可选）
- 信息揭示（可选）
- 段落计划（可选）
- 场景收束（可选）

## 属性
- id: `string`，必填。
- 地点引用: `string`，必填。必须命中 `地点.id`。
- 字数目标: `int`，可选。
- 节奏: `enum`，可选。`慢 | 中 | 快 | 慢转中 | 中转快`。
- 氛围: `csv<string>`，可选。
- 视角角色: `string`，可选。建议命中 `角色.id`。
