# 标签: 状态

## 作用
记录角色在段级的入/出状态快照，用于连续性与演化校验。

## 父标签
- 段

## 子标签
无。

## 属性
- 角色: `string`，必填。应命中 `角色.id`。
- 阶段: `enum`，必填。`入 | 出`。
- 体力: `enum`，可选。`极低 | 低 | 中 | 高`。
- 情绪: `enum`，可选。`平静 | 克制 | 紧绷 | 决绝 | 麻木`。

## 约束
- `状态校验=开启` 时，校验同角色跨段连续性。
- `体力演化=同级或下降` 时，不允许体力上升。
- `情绪演化=受限迁移` 时，按内置迁移矩阵校验。
